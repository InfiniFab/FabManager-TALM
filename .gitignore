# 🔁 Node.js modules
node_modules/

# 🔑 Fichiers sensibles
projects/.sshkeys/
credentials.json
.config.*
*.pem
*.key
*.crt

# 🛑 Node-RED runtime local
.sessions.json

# 🧹 Fichiers temporaires, logs, backups
*.log
*.tmp
*.bak
*.backup
*.swp
backups/

# 🧾 Données exports
*.sql
*.csv

# 🧠 Fichiers IDE / Système
.vscode/
.idea/
.DS_Store
Thumbs.db
cd ~/.node-red

# Supprimer de l'index Git tout ce qui est maintenant ignoré (pas du disque dur !)
git rm -r --cached .

# Réajouter tous les fichiers (en respectant le nouveau .gitignore)
git add .

# Faire un nouveau commit clair
git commit -m "🔧 Nettoyage Git: meilleur .gitignore + update flows"

# Pousser vers GitHub
git push

